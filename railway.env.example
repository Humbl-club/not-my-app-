# Railway Environment Variables Template
# Copy these to Railway's environment variables section

# Database (Railway will auto-generate PostgreSQL URL)
DATABASE_URL=postgresql://username:password@host:port/database

# Security Keys (Generate new ones for production!)
MASTER_ENCRYPTION_KEY=generate_new_64_char_hex_key_here
JWT_SECRET=generate_new_64_char_hex_key_here

# Frontend URL (Railway will provide this)
FRONTEND_URL=https://your-frontend-domain.railway.app

# Stripe Configuration (Get from Stripe Dashboard)
STRIPE_SECRET_KEY=sk_live_your_live_stripe_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_ID=price_your_price_id

# Email Configuration (Configure with your email service)
EMAIL_FROM=noreply@yourdomain.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Admin Configuration
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=YourSecurePassword123

# Application Settings
NODE_ENV=production
SESSION_TIMEOUT=1800000
MAX_FILE_SIZE=5242880
VISA_FEE_GBP=42.00